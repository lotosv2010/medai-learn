## 一、组件库的划分
我们的划分以`elementUi`为基准分为

+ `Basic`:`Button`、`Icon图标`、`Layout布局`、`container布局容器`...
+ `Form`: `Input`、`Radio`、`checkbox`、`DatePicker`、`Upload`...
+ `Data`:`Table`、`Tree`、`Pagination`...
+ `Notice`:`Alert`、`Loading`、`Message`...
+ `Navigation`: `Tabs`、`Dropdown`、`NavMenu`...
+ `Others`:`Popover`,`Dialog`、`inifiniteScroll`、`Carousel`...



## 二、通过`Vue-Cli`初始化项目
```javascript
vue create zh-ui
```



```javascript
? Check the features needed for your project:
 (*) Choose Vue version
 (*) Babel
 ( ) TypeScript
 ( ) Progressive Web App (PWA) Support
 ( ) Router
 ( ) Vuex
 (*) CSS Pre-processors
 ( ) Linter / Formatter
 (*) Unit Testing
 ( ) E2E Testing
```



```javascript
2.x
> 3.x (Preview)
```



```javascript
> Sass/SCSS (with dart-sass)  
  Sass/SCSS (with node-sass)
  Less
  Stylus
```



> 为什么选择[dart-sass](https://www.dart-china.org/t/topic/146)?
>



```javascript
? Pick a unit testing solution:
> Mocha + Chai # ui测试需要使用karma
  Jest
```



## 三、目录结构配置
```javascript
│  .browserslistrc # 兼容版本
│  .gitignore
│  babel.config.js # babel的配置文件
│  package-lock.json
│  package.json
│  README.md   
|  examples   # 组件使用案例
├─public
│      favicon.ico
│      index.html 
├─src
│  │  App.vue 
│  │  main.js
│  │  
│  ├─packages # 需要打包的组件
│  │      button
|  |      button-group
│  │      icon
│  │      index.js # 所有组件的入口
│  │       
│  └─styles # 公共样式
│         common
|         mixins
└─tests # 单元测试
    └─unit
          button.spec.js
```



<!-- 这是一张图片，ocr 内容为：-ui-next EXPLORER OPENEDITORS G-UI-NEXT config docs examples node_modules public packages button button-group icon index.ts I style ShowAllCommands Appvue main.ts GotoFile shiMs-Vue.D.ts FindinFILes tests .browserslistrc StartDebugging eslintrc.js gitignore ToggLeTerminaL babel.configjs packagejson + 中 PROBLEMS DEBUGCONSOLE OUTPUT TERMINAL :node README.md 5:47:32PM DONE Compiledsuccessfullyin116ms tsconfig-json vue.configJjs Apprunningat: yarn.lock Local:http://Localhost:2022 Network:http://10.6.24.141:2022 Issuescheckinginprogress.. Noissues found. TODOTREE:TODOS OUTLINE TIMELINE DEPENDENCIES ?040 -->
![](https://cdn.nlark.com/yuque/0/2021/png/738210/1610617807761-9f6d2ad2-9144-49e5-9e9b-53e39eed7785.png)



## 四、编写插件入口
`src/packages/index.ts`

```javascript
import "@/style/common/index.scss";
import Button from "./button";
import ButtonGroup from "./button-group";
import Icon from "./icon";

const plugins = [Button, ButtonGroup, Icon];

const install = (app: any) => {
  plugins.forEach(plugin => app.use(plugin));
};

export default {
  version: "1.0.0",
  install,
  Button,
  ButtonGroup,
  Icon
};
```



`src/main.ts`

```javascript
import { createApp } from "vue";
import App from "./App.vue";
import Gui from "./packages/index";

const app = createApp(App);
app.use(Gui);
app.mount("#app");
```



> 我们可以通过插件的方式去引入我们的组件库
>



