# 压缩和解压缩处理
+ 可以使用zlib模块进行压缩和解压缩处理，压缩文件以后可以减少体积，加速传输速度和节约带宽

# 压缩对象
+ 压缩和解压缩对象都是一个可写可读流

| **方法** | **说明** |
| --- | --- |
| zlib.createGzip | 返回Gzip流对象，使用Gzip算法对数据进行压缩处理 |
| zlib.createGunzip | 返回Gzip流对象，使用Gzip算法对压缩数据进行解压缩处理 |
| zlib.createDeflate | 返回Deflate流对象，使用Deflate算法对数据进行压缩处理 |
| zlib.createInflate | 返回Deflate流对象，使用Deflate算法对压缩数据进行解压缩处理 |


# 压缩和解压缩
```javascript
const fs = require("fs");
const zlib = require("zlib");
const path = require("path");

// 用于实现压缩 transform 转换流，继承自双工流
/**
 * 压缩
 * @param {*} src
 */
function gzip(src) {
  fs.createReadStream(src)
    .pipe(zlib.createGzip())
    .pipe(fs.createWriteStream(src + ".gz"));
}

/**
 * 解压
 * @param {*} src
 */
function gunzip(src) {
  fs.createReadStream(src)
    .pipe(zlib.createGunzip())
    .pipe(fs.createWriteStream(path.join(__dirname, path.basename(src, '.gz')))); // basename 文件名
}
// gzip(path.join(__dirname, 'msg.json'));
gunzip(path.join(__dirname, "msg.json.gz"));
```

# 在http中的应用
```javascript
const http = require('http');
const path = require('path');
const fs = require('fs');
const zlib = require('zlib');
const mime = require('mime');
const {promisify} = require('util');
const stat = promisify(fs.stat);

// 客户端向服务器发起请求的时候，
// 通过Accept-Encoding告诉服务器客户端支持的解压缩格式
// Accept-Encoding: gzip, deflate
const app = http.createServer(async (req, res) => {
  const {pathname} = new URL(req.url, `http://${req.headers.host}`);
  const filepath = path.join(__dirname, pathname);
  try {
    const statObj = await stat(filepath);
    // 获取Content-Type
    res.setHeader('Content-Type', mime.getType(pathname));
    // 为了兼容不同的浏览器，node把所有的请求头全部转换为小写
    const acceptEncoding = req.headers['accept-encoding'];
    // 协商内容
    if(acceptEncoding.match(/\bgzip\b/)) {
      res.setHeader('Content-Encoding', 'gzip');
      fs.createReadStream(filepath).pipe(zlib.createGzip()).pipe(res);
    } else if (acceptEncoding.match(/\bdeflate\b/)) {
      res.setHeader('Content-Encoding', 'deflate');
      fs.createReadStream(filepath).pipe(zlib.createDeflate()).pipe(res);
    } else {
      fs.createReadStream(filepath).pipe(res);
    }
  } catch (error) {
    res.statusCode = 404;
    res.end('NOT FOUND');
  }
});
app.listen(8989);
```

```json
{
  "name": "zip",
  "version": "1.0.0",
  "description": "",
  "main": "gzip.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "gzip": "node gzip.js",
    "server": "nodemon server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mime": "^3.0.0"
  }
}

```

